[project]
name = "sign-os-worker"
version = "0.1.0"
description = "Sign Language OS — Celery Workers"
authors = [
    {name = "SignOS Team"}
]
requires-python = "^3.11"
dependencies = [
    "celery (>=5.6.3,<6.0.0)",
    "redis (>=7.4.0,<8.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
